<!--
  Draft-PR description template. Placeholders use {{kebab-case}} syntax and are
  substituted by the draft-PR-creator stage at runtime — NOT by the Claude model.
  Sections with no content are pre-rendered to empty strings by the stage.

  Format follows the team's PR house style (the `fw-step4-pullRequest` and
  `fw-create-pr` skills): change bullets first, then a test-environment block
  separated by `---`. Deliberately absent, because those skills forbid them:
  a file-changed list, per-stage agent narration, and any tool/agent attribution.
-->
{{coder-bullets}}
{{reviewer-section}}
{{test-environment-section}}
